<?php

/*

// For an arbitrary string $mystring, which of the following checks will correctly determine if the string PHP exists within it?

1) if(strpos($mystring, "PHP") !== false)							http://php.net/strpos
2) if(!strpos($mystring,"PHP"))
3) if(strpos($mystring, "PHP") === true)
4) if(strloc($mystring, "PHP") == true)
5) if(strloc($mystring, "PHP") === false)

*/

