<?php

/*

// When writing CLI scripts it is often useful to access the standard streams available to the operating system such as standard input/output and error.
// How does one access these streams in PHP 5?

1) Use stdin(), stdout() and stderr() functions
2) PHP::STDIN, PHP::STDOUT, PHP::STDERR class constants in PHP 5
3) STDIN, STDOUT, and STDERR constants in PHP 5								// http://php.net/manual/en/features.commandline.io-streams.php
4) use the php::stdin(), php::stdout(), and php::stderr() class methods

*/

