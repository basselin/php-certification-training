<?php

// When uploading a file using HTTP, which variable can be used to locate the file on PHP's local filesystem?

/*

1) None of the above
2) $_FILES['fieldname']['tmp_name']					http://php.net/manual/en/function.move-uploaded-file.php
3) $_FILES['fieldname']
4) $_FILES['fieldname'][0]['filename']
5) $_FILES['fieldname']['filename']

*/

